\ProvidesPackage{mymacro}[2017/01/03 v1.0 My own macro]

% new commands
% ----------

% \breakcell[pos]{content}
% creates a mini table to contain the contents so that one can put multiple rows of text in one cell.
% pos: b,c(default),t; vertical position of the content inside breakcell: b for bottom, c for center, and t for top.
% content: content of the breakcell, accepts common tabular commands like &, \\, \hline, \newline, and \cline{i-j}.
\newcommand{\breakcell}[2][t]{\begin{tabular}[#1]{@{}l}#2\end{tabular}}

\endinput
